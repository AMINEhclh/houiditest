## Packages
framer-motion | For smooth scroll animations and entry effects

## Notes
- The backend schema supports inquiries, but the current UI requirements specify a "WhatsApp only" workflow (NO forms).
- The `useInquiries` hook will be generated for future compatibility but not used in the landing page view.
- Images will be purely programmatic (gradients/icons) or external (Unsplash) as needed.
